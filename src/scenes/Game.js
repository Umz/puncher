import Phaser from "phaser";
import Text from "../common/Text";

export default class Game extends Phaser.Scene {
      
    constructor() {
        super(Text.GAME);
    }
    
    create () {
    }

    update(time, delta) {
    }
    
}