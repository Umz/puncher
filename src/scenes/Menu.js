import Phaser from "phaser";
import Text from "../common/Text";

export default class Menu extends Phaser.Scene {
    
    constructor() {
        super(Text.MENU);
    }
    
    create () {
    }

    update(time, delta) {
    }
    
}